[project]
name = "rss-playground"
version = "0.1.0"
description = "YouTube channel tracker"
requires-python = ">=3.12"
dependencies = [
    "prek>=0.3.6",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]

[tool.pytest.ini_options]
testpaths = ["scripts/tests"]
pythonpath = ["scripts"]
